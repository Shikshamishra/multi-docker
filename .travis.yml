language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t shikshamishra/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true shikshamishra/react-test npm run test -- --coverage

after_success:
  - docker build -t shikshamishra/multi-client ./client
  - docker build -t shikshamishra/multi-nginx ./nginx
  - docker build -t shikshamishra/multi-server ./server
  - docker build -t shikshamishra/multi-worker ./worker
  #Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  #Take those images and push them to docker hub
  - docker push shikshamishra/multi-client
  - docker push shikshamishra/multi-nginx
  - docker push shikshamishra/multi-server
  - docker push shikshamishra/multi-worker
